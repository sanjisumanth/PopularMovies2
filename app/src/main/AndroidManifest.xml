<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sumanthkrishna.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".MainActivity"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".DetailMovieActivity"
            android:launchMode="singleTop"/>

        <provider
            android:authorities="com.example.sumanthkrishna.popularmovies"
            android:name=".data.MoviesProvider"
            android:exported="false"/>
        <service android:name=".sync.MovieSyncIntentService"
            android:exported="false"/>
        <service android:name=".sync.ReviewsSyncIntentService"
            android:exported="false"/>
        <service android:name=".sync.TailerSyncIntentService"
            android:exported="false"/>

    </application>

</manifest>